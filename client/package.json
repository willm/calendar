{
  "name": "simplecal",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "script.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "debug": "vitest --inspect-brk --pool threads --poolOptions.threads.singleThread",
    "test": "vitest",
    "test-once": "vitest --run",
    "coverage": "vitest --coverage",
    "lint": "prettier -c ../prettierrc.json --check .",
    "format": "prettier  -c ../prettierrc.json --write .",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@js-temporal/polyfill": "^0.4.4",
    "ical": "^0.8.0",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "vite": "^5.0.12"
  }
}
